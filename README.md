# firmware-pipeline

Automated CI/CD pipeline that builds EDK2 firmware and boot-tests it in QEMU on every push and pull request.

OpenBMC (BMC-side) CI is planned as a next milestone, added alongside EDK2 in this same repo once the EDK2 pipeline is solid.

